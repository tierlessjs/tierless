--- a/js/apps/admin-ui/test/utils/constants.ts
+++ b/js/apps/admin-ui/test/utils/constants.ts
@@ -1,4 +1,9 @@
-export const SERVER_URL = "http://localhost:8080";
+// tierless corpus harness (test patch, both arms): measured runs route the BROWSER
+// through a counting/shaping relay, so the origin the suite navigates to has to move.
+// test/utils/AdminClient.ts keeps its own hardcoded http://localhost:8080, which is
+// what we want — node-side seeding must never be counted as page traffic.
+export const SERVER_URL =
+  process.env.TIERLESS_BASE_URL || "http://localhost:8080";
 export const ROOT_PATH = "/admin/:realm/console";
 export const DEFAULT_REALM = "master";
 export const ADMIN_USER = "admin";
