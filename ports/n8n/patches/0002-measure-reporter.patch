--- a/packages/testing/playwright/playwright.config.ts
+++ b/packages/testing/playwright/playwright.config.ts
@@ -71,7 +71,10 @@
 	});
 }
 
-if (FRONTEND_URL) {
+// tierless corpus harness (test patch, both arms): the skip covers BOTH entries — a
+// measured run boots n8n itself and points the browser at a relay origin, which this
+// entry would otherwise probe (reuseExistingServer=false in dev mode) and refuse
+if (FRONTEND_URL && !SKIP_WEB_SERVER) {
 	webServer.push({
 		command: 'cd .. && pnpm dev:fe:editor',
 		url: `${FRONTEND_URL}/favicon.ico`,
@@ -102,9 +105,11 @@
 
 	use: {
 		baseURL: WEB_SERVER_URL,
-		trace: 'on',
-		video: 'on',
-		screenshot: 'on',
+		// tierless corpus harness (test patch, both arms): recording costs sit inside
+		// every measured duration — off on measured runs, identically in both arms
+		trace: process.env.TIERLESS_MEASURE_OUT ? 'off' : 'on',
+		video: process.env.TIERLESS_MEASURE_OUT ? 'off' : 'on',
+		screenshot: process.env.TIERLESS_MEASURE_OUT ? 'off' : 'on',
 		testIdAttribute: 'data-test-id',
 		headless: process.env.SHOW_BROWSER !== 'true',
 		viewport: MACBOOK_WINDOW_SIZE,
@@ -113,8 +118,11 @@
 		currentsFixturesEnabled: !!process.env.CI,
 	},
 
-	reporter: IS_CI
-		? [
+	// tierless corpus harness (test patch, both arms): per-test JSONL when measuring
+	reporter: process.env.TIERLESS_MEASURE_OUT
+		? ([['line'], ['./tierlessMeasureReporter.ts']] as const)
+		: IS_CI
+			? [
 				['list'],
 				['junit', { outputFile: process.env.PLAYWRIGHT_JUNIT_OUTPUT_NAME ?? 'results.xml' }],
 				['html', { open: 'never' }],
--- a/packages/testing/playwright/tierlessMeasureReporter.ts
+++ b/packages/testing/playwright/tierlessMeasureReporter.ts
@@ -0,0 +1,67 @@
+// Per-test measurement reporter (tierless corpus harness, docs/corpus.md): appends one
+// JSONL row per test ATTEMPT to TIERLESS_MEASURE_OUT — id, status, retry, durationMs.
+// With TIERLESS_WIRE_URLS set (comma-separated endpoints returning flat JSON number
+// counters), reads them around each attempt and records the deltas as wire* fields.
+// Measured lanes run workers=1, so no two tests generate traffic at once; but
+// Playwright does NOT await async reporter hooks, so a snapshot can lag its boundary
+// and mis-attribute between ADJACENT tests — per-test deltas are best-effort (fine
+// for medians), while the byte instrument of record is the TCP-true pair of runs.
+// A counter read that fails INVALIDATES the row (wireError: true, no wire fields)
+// instead of shipping wrong deltas — a missed BEGIN read would silently attribute all
+// prior suite traffic to this test; report.mts excludes flagged rows and says so.
+import type { Reporter, TestCase, TestResult } from '@playwright/test/reporter';
+import { appendFileSync } from 'fs';
+
+const OUT = process.env.TIERLESS_MEASURE_OUT;
+const WIRE = (process.env.TIERLESS_WIRE_URLS || '').split(',').filter(Boolean);
+
+// null = a configured endpoint was unreachable or non-2xx: this attempt has no valid deltas
+async function counters(): Promise<Record<string, number> | null> {
+	const out: Record<string, number> = {};
+	for (const url of WIRE) {
+		try {
+			const r = await fetch(url);
+			if (!r.ok) return null;
+			const j = (await r.json()) as Record<string, unknown>;
+			for (const [k, v] of Object.entries(j)) if (typeof v === 'number') out[k] = (out[k] || 0) + v;
+		} catch {
+			return null;
+		}
+	}
+	return out;
+}
+
+class TierlessMeasure implements Reporter {
+	private before: Record<string, number> | null = {};
+	async onTestBegin(_test: TestCase) {
+		if (WIRE.length) this.before = await counters();
+	}
+	async onTestEnd(test: TestCase, result: TestResult) {
+		if (!OUT) return;
+		const after = WIRE.length ? await counters() : {};
+		const wire: Record<string, number | boolean> = {};
+		if (WIRE.length && (!this.before || !after)) {
+			wire.wireError = true;
+		} else if (after) {
+			for (const k of Object.keys(after))
+				wire['wire' + k[0].toUpperCase() + k.slice(1)] = after[k] - (this.before![k] || 0);
+		}
+		const file = test.location.file.replace(/^.*packages\/testing\/playwright\//, '');
+		const titles = test.titlePath().filter((t) => t && !t.endsWith('.spec.ts') && t !== 'e2e');
+		appendFileSync(
+			OUT,
+			JSON.stringify({
+				id: `${file}:${test.location.line} › ${titles.join(' › ')}`,
+				status: result.status,
+				retry: result.retry,
+				durationMs: result.duration,
+				...wire,
+			}) + '\n',
+		);
+	}
+	printsToStdio() {
+		return false;
+	}
+}
+// eslint-disable-next-line import-x/no-default-export
+export default TierlessMeasure;
