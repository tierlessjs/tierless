{
  "name": "stackmix-app",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "stackmix build app.src.js app.gen.mjs --bare",
    "dev": "npm run build && node server.mjs",
    "explain": "stackmix explain app.src.js",
    "check:api": "stackmix api api.server.mjs"
  },
  "dependencies": {
    "stackmix": "^0.1.0"
  }
}
