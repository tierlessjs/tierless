{
  "name": "create-tierless",
  "version": "0.1.0",
  "type": "module",
  "description": "Scaffold a new Tierless app: one tierless program, a trusted api service, a two-tier host — running in under a minute.",
  "bin": {
    "create-tierless": "index.mjs"
  },
  "files": [
    "index.mjs",
    "template"
  ],
  "license": "Apache-2.0",
  "engines": {
    "node": ">=22.18.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "@types/node": "^26.1.0"
  }
}
