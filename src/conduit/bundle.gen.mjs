// GENERATED by transform.cjs from src/conduit/App.src.js — do not edit by hand.
import { h, render, HomeView, ArticleView, EditorView } from "./view.mjs";
function loadEditor(draft, error) { return render(h(EditorView, { draft: draft, error: error })); }
export const PROGRAMS = {
  loadHome(F) {
    while (true) switch (F.pc) {
      case 0:
        F.pc = 6; break;
      case 1:
        return { op: "return", value: "(end)" };
      case 2:
        return { op: "return", value: render(h(HomeView, { tags: F.tags, feed: F.feed, tag: F.args[0] })) };
      case 3:
        F.feed = F.ret;
        F.pc = 2; break;
      case 4:
        F.pc = 3; return { op: "resource", tier: "server", name: "api.feed", args: [F.args[0]] };
      case 5:
        F.tags = F.ret;
        F.pc = 4; break;
      case 6:
        F.pc = 5; return { op: "resource", tier: "server", name: "api.getTags", args: [] };
      default: throw new RangeError("stackmix: invalid pc " + F.pc + " in loadHome (forged continuation?)");
    }
  },
  loadArticle(F) {
    while (true) switch (F.pc) {
      case 0:
        F.pc = 6; break;
      case 1:
        return { op: "return", value: "(end)" };
      case 2:
        return { op: "return", value: render(h(ArticleView, { article: F.article, comments: F.comments })) };
      case 3:
        F.comments = F.ret;
        F.pc = 2; break;
      case 4:
        F.pc = 3; return { op: "resource", tier: "server", name: "api.getComments", args: [F.args[0]] };
      case 5:
        F.article = F.ret;
        F.pc = 4; break;
      case 6:
        F.pc = 5; return { op: "resource", tier: "server", name: "api.getArticle", args: [F.args[0]] };
      default: throw new RangeError("stackmix: invalid pc " + F.pc + " in loadArticle (forged continuation?)");
    }
  },
  App(F) {
    while (true) switch (F.pc) {
      case 0:
        F.pc = 54; break;
      case 1:
        return { op: "return", value: "(end)" };
      case 2:
        return { op: "return", value: "session ended" };
      case 3:
        F.pc = 49; break;
      case 4:
        F.route = "article";
        F.pc = 3; break;
      case 5:
        F.slug = F.ev.slug;
        F.pc = 4; break;
      case 6:
        F.route = "home";
        F.pc = 3; break;
      case 7:
        F.tag = F.ev.tag;
        F.pc = 6; break;
      case 8:
        F.route = "home";
        F.pc = 3; break;
      case 9:
        F.tag = "";
        F.pc = 8; break;
      case 10:
        F.pc = 3; break;
      case 11:
        F.pc = 10; return { op: "resource", tier: "server", name: "api.toggleFavorite", args: [F.slug] };
      case 12:
        F.pc = 3; break;
      case 13:
        F.pc = 12; return { op: "resource", tier: "server", name: "api.addComment", args: [F.slug, F.ev.body] };
      case 14:
        F.pc = 3; break;
      case 15:
        F.pc = 14; return { op: "resource", tier: "server", name: "api.deleteComment", args: [F.ev.id] };
      case 16:
        F.route = "editor";
        F.pc = 3; break;
      case 17:
        F.draft = { title: "", body: "", tags: "" };
        F.pc = 16; break;
      case 18:
        (F.__h || (F.__h = [])).push({ catch: 19, fin: null, state: 0 }); F.pc = 24; break;
      case 19:
        F.e = F.__err;
        F.pc = 28; break;
      case 20:
        F.__h.pop();
        F.pc = 3; break;
      case 21:
        F.route = "article";
        F.pc = 20; break;
      case 22:
        // validates on the server; throws on a blank title
F.slug = F.created.slug;
        F.pc = 21; break;
      case 23:
        F.created = F.ret;
        F.pc = 22; break;
      case 24:
        F.pc = 23; return { op: "resource", tier: "server", name: "api.publish", args: [F.ev.title, F.ev.body, F.ev.tags] };
      case 25:
        F.__h.pop();
        F.pc = 3; break;
      case 26:
        F.route = "editor";
        F.pc = 25; break;
      case 27:
        // the server-tier throw is caught here, across the boundary
F.draft = { title: F.ev.title, body: F.ev.body, tags: F.ev.tags };
        F.pc = 26; break;
      case 28:
        F.error = F.e.message; // the server-tier throw is caught here, across the boundary
        F.pc = 27; break;
      case 29:
        F.pc = 2; break;
      case 30:
        if (F.ev.ev === "publish") { F.pc = 18; } else { F.pc = 29; } break;
      case 31:
        if (F.ev.ev === "new") { F.pc = 17; } else { F.pc = 30; } break;
      case 32:
        if (F.ev.ev === "uncomment") { F.pc = 15; } else { F.pc = 31; } break;
      case 33:
        if (F.ev.ev === "comment") { F.pc = 13; } else { F.pc = 32; } break;
      case 34:
        if (F.ev.ev === "favorite") { F.pc = 11; } else { F.pc = 33; } break;
      case 35:
        if (F.ev.ev === "home") { F.pc = 9; } else { F.pc = 34; } break;
      case 36:
        if (F.ev.ev === "tag") { F.pc = 7; } else { F.pc = 35; } break;
      case 37:
        if (F.ev.ev === "open") { F.pc = 5; } else { F.pc = 36; } break;
      case 38:
        F.error = "";
        F.pc = 37; break;
      case 39:
        F.ev = F.ret;
        F.pc = 38; break;
      case 40:
        F.pc = 39; return { op: "resource", tier: "browser", name: "dom.commit", args: [F.vdom] };
      case 41:
        F.vdom = F.__t0;
        F.pc = 40; break;
      case 42:
        F.__t0 = F.ret;
        F.pc = 41; break;
      case 43:
        F.pc = 42; return { op: "call", fn: "loadHome", args: [F.tag] };
      case 44:
        F.vdom = F.__t1;
        F.pc = 40; break;
      case 45:
        F.__t1 = F.ret;
        F.pc = 44; break;
      case 46:
        F.pc = 45; return { op: "call", fn: "loadArticle", args: [F.slug] };
      case 47:
        F.vdom = loadEditor(F.draft, F.error);
        F.pc = 40; break;
      case 48:
        if (F.route === "article") { F.pc = 46; } else { F.pc = 47; } break;
      case 49:
        if (F.route === "home") { F.pc = 43; } else { F.pc = 48; } break;
      case 50:
        F.error = "";
        F.pc = 3; break;
      case 51:
        F.draft = { title: "", body: "", tags: "" };
        F.pc = 50; break;
      case 52:
        F.slug = "";
        F.pc = 51; break;
      case 53:
        F.tag = "";
        F.pc = 52; break;
      case 54:
        F.route = "home";
        F.pc = 53; break;
      default: throw new RangeError("stackmix: invalid pc " + F.pc + " in App (forged continuation?)");
    }
  }
};

// A §5 handle — a big local that stayed on its owning tier (see ../heap.mjs). With
// --auto-deref the machine guards reads of remotable locals with this check.
export const isHandle = (x) => x !== null && typeof x === "object" && x.__stackmix_handle__ === true;
// Exception dispatch over the serializable handler stack F.__h. Returns the pc of the
// catch/finally to enter, or null if the throw escapes this frame. Called from the
// machine (for `throw`) and from the runtime (when a migrated resource throws).
export function __dispatch(F, err) {
  const hs = F.__h;
  while (hs && hs.length) {
    const h = hs[hs.length - 1];
    if (h.catch != null && h.state === 0) { h.state = 1; F.__err = err; return h.catch; }  // enter catch (handler stays for a wrapping finally)
    if (h.fin != null && h.state < 2) { h.state = 2; F.__c = { type: "throw", arg: err }; return h.fin; }
    hs.pop();
  }
  return null;
}
// Drive a pending return/break/continue (F.__c) through the finallys it must still run:
// pop the handlers of crossed try/catch regions, then return the next finally pc (or null
// when none remain and the completion should execute).
export function __unwindStep(F) {
  const c = F.__c; if (!c || !c.steps) return null;
  while (c.steps.length && c.steps[0].pop !== undefined) { for (let i = 0; i < c.steps[0].pop; i++) F.__h.pop(); c.steps.shift(); }
  if (c.steps.length) return c.steps.shift().fin;
  return null;
}
// Unwind an error across FRAMES: try the top frame's handlers, else pop it and try the
// caller. A resource that fails in a callee is thus caught by a try/catch in a caller.
export function __unwind(stack, err) {
  while (stack.length) { const tpc = __dispatch(stack[stack.length - 1], err); if (tpc != null) { stack[stack.length - 1].pc = tpc; return true; } stack.pop(); }
  return false;
}
// Single-tier driver: step the machine, push sub-frames for calls, stop at every resource
// request. The two-tier runtime (../runtime.mjs) drives PROGRAMS directly and only stops
// at resources THIS tier doesn't own; this local driver keeps the bundle runnable alone.
export function run(stack) {
  for (;;) {
    const top = stack[stack.length - 1];
    const r = PROGRAMS[top.fn](top);
    if (r.op === "return") { stack.pop(); if (!stack.length) return { done: true, value: r.value }; stack[stack.length - 1].ret = r.value; }
    else if (r.op === "call") { stack.push({ fn: r.fn, pc: 0, args: r.args }); }
    else if (r.op === "throw") { stack.pop(); if (!__unwind(stack, r.value)) throw r.value; }
    else if (r.op === "resource") return { done: false, request: r, stack };
  }
}
export const start = (fn, args = []) => run([{ fn, pc: 0, args }]);
